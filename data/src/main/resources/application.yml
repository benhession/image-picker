"%dev":
  bucket:
    name: "test-bucket"

  quarkus:
    s3:
      devservices:
        buckets: "test-bucket"
      aws:
        credentials:
          type: "static"
          static-provider:
            access-key-id: "test-key"
            secret-access-key: "test-secret"

"%test":
  bucket:
    name: "test-bucket"

  quarkus:
    s3:
      devservices:
        buckets: "test-bucket"
      aws:
        credentials:
          type: "static"
          static-provider:
            access-key-id: "test-key"
            secret-access-key: "test-secret"
    mongodb:
      devservices:
        enabled: false
